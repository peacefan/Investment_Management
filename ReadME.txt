This app is built with nodeJs framework and sql database.

Database is hanndled and build in XAMPP. 
In database folder, the SQL can be copied over to host the needed information, kindly name the databse to (mydb).

after starting the database on XAMPP:
1. go to the main folder and user the command [npm install]
2. to start the project use [npm run devStart]
